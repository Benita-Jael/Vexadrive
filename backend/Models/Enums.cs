namespace VexaDriveAPI.Models
{
    public enum UserRole
    {
        Customer = 0,
        Admin = 1
    }

    public enum ServiceStatus
    {
        RequestCreated = 0,
        ServiceInProgress = 1,
        ServiceCompleted = 2
    }
}
