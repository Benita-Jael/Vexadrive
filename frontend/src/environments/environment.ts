export const environment = {
  production: false,
  // Update this to match your API URL (include scheme and port if using HTTPS)
  // Match the backend launchSettings applicationUrl (http, port 5066)
  apiBaseUrl: 'http://localhost:5066'
};
